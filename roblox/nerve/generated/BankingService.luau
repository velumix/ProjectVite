--!strict

-- Generated by nerve-contract-codegen.mjs. Do not edit by hand.
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Nerve = require(ReplicatedStorage.Nerve)
local S = Nerve.Schema

local BankingService = Nerve.CreateService({
	Name = "BankingService",
	Client = {
		BankingChanged = Nerve.Signal(S.Tuple({ S.Struct({ kind = S.String, amount = S.Float64, currency = S.String, sender = S.Optional(S.String), label = S.Optional(S.String) }) }), { Direction = "server" }),
	},
	Network = {
		GetBankingOverview = Nerve.Method({ Request = S.Tuple({  }), Response = S.Tuple({ S.Struct({ bank = S.Float64, cash = S.Float64, currency = S.String, playerId = S.Int32, playerName = S.String, transactions = S.Array(S.Struct({ id = S.String, kind = S.String, amount = S.Float64, label = S.String, reference = S.String, createdAt = S.Float64 })) }) }) }),
		TransferMoney = Nerve.Method({ Request = S.Struct({ amount = S.Float64, phoneNumber = S.String, note = S.Optional(S.String) }), Response = S.Tuple({ S.Boolean, S.Optional(S.String) }), RateLimit = { Requests = 10, Window = 1 } }),
		DepositMoney = Nerve.Method({ Request = S.Struct({ amount = S.Float64 }), Response = S.Tuple({ S.Boolean, S.Optional(S.String) }), RateLimit = { Requests = 10, Window = 1 } }),
		WithdrawMoney = Nerve.Method({ Request = S.Struct({ amount = S.Float64 }), Response = S.Tuple({ S.Boolean, S.Optional(S.String) }), RateLimit = { Requests = 10, Window = 1 } }),
	},
})

function BankingService.Client:GetBankingOverview(_player: Player)
	-- Implement the authoritative server behavior here.
	return nil
end

function BankingService.Client:TransferMoney(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function BankingService.Client:DepositMoney(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function BankingService.Client:WithdrawMoney(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function BankingService:NerveInit()
end

function BankingService:NerveStart()
end

return BankingService
