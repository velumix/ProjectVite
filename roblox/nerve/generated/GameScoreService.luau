--!strict

-- Generated by nerve-contract-codegen.mjs. Do not edit by hand.
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Nerve = require(ReplicatedStorage.Nerve)
local S = Nerve.Schema

local GameScoreService = Nerve.CreateService({
	Name = "GameScoreService",
	Client = {
		ScoreUpdated = Nerve.Signal(S.Tuple({ S.Struct({ gameId = S.String, highScore = S.Float64 }) }), { Direction = "server" }),
	},
	Network = {
		GetGameScores = Nerve.Method({ Request = S.Struct({  }), Response = S.Tuple({ S.Map(S.String, S.Float64) }) }),
		SubmitScore = Nerve.Method({ Request = S.Struct({ gameId = S.String, score = S.Float64 }), Response = S.Tuple({ S.Boolean, S.Optional(S.String), S.Optional(S.Float64) }) }),
		ResetScore = Nerve.Method({ Request = S.Struct({ gameId = S.String }), Response = S.Tuple({ S.Boolean, S.Optional(S.String) }) }),
	},
})

function GameScoreService.Client:GetGameScores(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function GameScoreService.Client:SubmitScore(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function GameScoreService.Client:ResetScore(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function GameScoreService:NerveInit()
end

function GameScoreService:NerveStart()
end

return GameScoreService
