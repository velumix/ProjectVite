--!strict

-- Generated by nerve-contract-codegen.mjs. Do not edit by hand.
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Nerve = require(ReplicatedStorage.Nerve)
local S = Nerve.Schema

local FlareService = Nerve.CreateService({
	Name = "FlareService",
	Client = {
		NewMatch = Nerve.Signal(S.Tuple({ S.Struct({ id = S.String, name = S.String, age = S.Float64, bio = S.String, distanceKm = S.Float64, interests = S.Array(S.String), isMatch = S.Boolean }) }), { Direction = "server" }),
		NewFlareMessage = Nerve.Signal(S.Tuple({ S.Struct({ id = S.String, matchId = S.String, sender = S.String, text = S.String, timestamp = S.Float64 }) }), { Direction = "server" }),
	},
	Network = {
		GetProfiles = Nerve.Method({ Request = S.Struct({  }), Response = S.Tuple({ S.Array(S.Struct({ id = S.String, name = S.String, age = S.Float64, bio = S.String, distanceKm = S.Float64, interests = S.Array(S.String), isMatch = S.Boolean })) }) }),
		SwipeProfile = Nerve.Method({ Request = S.Struct({ profileId = S.String, direction = S.String }), Response = S.Tuple({ S.Boolean, S.Optional(S.String), S.Optional(S.Boolean) }) }),
		GetMessages = Nerve.Method({ Request = S.Struct({ matchId = S.String }), Response = S.Tuple({ S.Array(S.Struct({ id = S.String, matchId = S.String, sender = S.String, text = S.String, timestamp = S.Float64 })) }) }),
		SendMessage = Nerve.Method({ Request = S.Struct({ matchId = S.String, text = S.String }), Response = S.Tuple({ S.Boolean, S.Optional(S.String), S.Optional(S.Struct({ id = S.String, matchId = S.String, sender = S.String, text = S.String, timestamp = S.Float64 })) }) }),
	},
})

function FlareService.Client:GetProfiles(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function FlareService.Client:SwipeProfile(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function FlareService.Client:GetMessages(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function FlareService.Client:SendMessage(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function FlareService:NerveInit()
end

function FlareService:NerveStart()
end

return FlareService
