--!strict

-- Generated by nerve-contract-codegen.mjs. Do not edit by hand.
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Nerve = require(ReplicatedStorage.Nerve)
local S = Nerve.Schema

local MailService = Nerve.CreateService({
	Name = "MailService",
	Client = {
		MailChanged = Nerve.Signal(S.Tuple({ S.String }), { Direction = "server" }),
	},
	Network = {
		GetMailbox = Nerve.Method({ Request = S.Struct({ folder = S.Optional(S.String), query = S.Optional(S.String) }), Response = S.Tuple({ S.Array(S.Struct({ id = S.String, sender = S.String, senderName = S.String, senderAddress = S.String, recipient = S.String, subject = S.String, body = S.String, timestamp = S.Float64, read = S.Boolean, folder = S.String, starred = S.Boolean })), S.Struct({ inboxCount = S.Float64, unreadCount = S.Float64, sentCount = S.Float64, trashCount = S.Float64 }) }) }),
		SendMail = Nerve.Method({ Request = S.Struct({ to = S.String, subject = S.String, body = S.String }), Response = S.Tuple({ S.Boolean, S.Optional(S.Struct({ id = S.String, sender = S.String, senderName = S.String, senderAddress = S.String, recipient = S.String, subject = S.String, body = S.String, timestamp = S.Float64, read = S.Boolean, folder = S.String, starred = S.Boolean })), S.Optional(S.String) }) }),
		MarkMailRead = Nerve.Method({ Request = S.Struct({ ids = S.Array(S.String), read = S.Boolean }), Response = S.Tuple({ S.Boolean, S.Optional(S.String) }) }),
		ToggleMailStar = Nerve.Method({ Request = S.Struct({ id = S.String }), Response = S.Tuple({ S.Boolean, S.Boolean, S.Optional(S.String) }) }),
		DeleteMail = Nerve.Method({ Request = S.Struct({ ids = S.Array(S.String), permanent = S.Optional(S.Boolean) }), Response = S.Tuple({ S.Boolean, S.Optional(S.String) }) }),
	},
})

function MailService.Client:GetMailbox(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function MailService.Client:SendMail(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function MailService.Client:MarkMailRead(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function MailService.Client:ToggleMailStar(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function MailService.Client:DeleteMail(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function MailService:NerveInit()
end

function MailService:NerveStart()
end

return MailService
