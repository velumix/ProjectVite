--!strict

-- Generated by nerve-contract-codegen.mjs. Do not edit by hand.
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Nerve = require(ReplicatedStorage.Nerve)
local S = Nerve.Schema

local RadioService = Nerve.CreateService({
	Name = "RadioService",
	Client = {
		RadioStateChanged = Nerve.Signal(S.Tuple({ S.Struct({ connected = S.Boolean, frequency = S.Float64, volume = S.Float64, micMuted = S.Boolean, isTalking = S.Boolean, channelMembers = S.Array(S.String), activeSpeakers = S.Array(S.String), presets = S.Array(S.Struct({ name = S.String, freq = S.Float64 })) }) }), { Direction = "server" }),
		SpeakerTalkingChanged = Nerve.Signal(S.Tuple({ S.Struct({ speaker = S.String, isTalking = S.Boolean, activeSpeakers = S.Array(S.String) }) }), { Direction = "server" }),
	},
	Network = {
		GetRadioState = Nerve.Method({ Request = S.Struct({  }), Response = S.Tuple({ S.Struct({ connected = S.Boolean, frequency = S.Float64, volume = S.Float64, micMuted = S.Boolean, isTalking = S.Boolean, channelMembers = S.Array(S.String), activeSpeakers = S.Array(S.String), presets = S.Array(S.Struct({ name = S.String, freq = S.Float64 })) }) }) }),
		ConnectFrequency = Nerve.Method({ Request = S.Struct({ frequency = S.Float64 }), Response = S.Tuple({ S.Boolean, S.Optional(S.String), S.Optional(S.Struct({ connected = S.Boolean, frequency = S.Float64, volume = S.Float64, micMuted = S.Boolean, isTalking = S.Boolean, channelMembers = S.Array(S.String), activeSpeakers = S.Array(S.String), presets = S.Array(S.Struct({ name = S.String, freq = S.Float64 })) })) }) }),
		DisconnectRadio = Nerve.Method({ Request = S.Struct({  }), Response = S.Tuple({ S.Boolean, S.Optional(S.String), S.Optional(S.Struct({ connected = S.Boolean, frequency = S.Float64, volume = S.Float64, micMuted = S.Boolean, isTalking = S.Boolean, channelMembers = S.Array(S.String), activeSpeakers = S.Array(S.String), presets = S.Array(S.Struct({ name = S.String, freq = S.Float64 })) })) }) }),
		SetVolume = Nerve.Method({ Request = S.Struct({ volume = S.Float64 }), Response = S.Tuple({ S.Boolean, S.Optional(S.String), S.Optional(S.Float64) }) }),
		ToggleMute = Nerve.Method({ Request = S.Struct({  }), Response = S.Tuple({ S.Boolean, S.Optional(S.String), S.Optional(S.Boolean) }) }),
		PushToTalk = Nerve.Method({ Request = S.Struct({ isTalking = S.Boolean }), Response = S.Tuple({ S.Boolean, S.Optional(S.String), S.Optional(S.Boolean) }) }),
	},
})

function RadioService.Client:GetRadioState(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function RadioService.Client:ConnectFrequency(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function RadioService.Client:DisconnectRadio(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function RadioService.Client:SetVolume(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function RadioService.Client:ToggleMute(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function RadioService.Client:PushToTalk(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function RadioService:NerveInit()
end

function RadioService:NerveStart()
end

return RadioService
