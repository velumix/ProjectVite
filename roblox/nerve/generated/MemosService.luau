--!strict

-- Generated by nerve-contract-codegen.mjs. Do not edit by hand.
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Nerve = require(ReplicatedStorage.Nerve)
local S = Nerve.Schema

local MemosService = Nerve.CreateService({
	Name = "MemosService",
	Client = {
		MemoSaved = Nerve.Signal(S.Tuple({ S.Struct({ id = S.String, title = S.String, durationSec = S.Float64, date = S.String, waveform = S.Array(S.Float64), notes = S.String }) }), { Direction = "server" }),
		MemoDeleted = Nerve.Signal(S.Tuple({ S.Struct({ id = S.String }) }), { Direction = "server" }),
	},
	Network = {
		GetMemos = Nerve.Method({ Request = S.Struct({  }), Response = S.Tuple({ S.Array(S.Struct({ id = S.String, title = S.String, durationSec = S.Float64, date = S.String, waveform = S.Array(S.Float64), notes = S.String })) }) }),
		SaveMemo = Nerve.Method({ Request = S.Struct({ title = S.String, durationSec = S.Float64, notes = S.String }), Response = S.Tuple({ S.Boolean, S.Optional(S.String), S.Optional(S.Struct({ id = S.String, title = S.String, durationSec = S.Float64, date = S.String, waveform = S.Array(S.Float64), notes = S.String })) }) }),
		DeleteMemo = Nerve.Method({ Request = S.Struct({ id = S.String }), Response = S.Tuple({ S.Boolean, S.Optional(S.String), S.Optional(S.String) }) }),
	},
})

function MemosService.Client:GetMemos(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function MemosService.Client:SaveMemo(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function MemosService.Client:DeleteMemo(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function MemosService:NerveInit()
end

function MemosService:NerveStart()
end

return MemosService
