--!strict

-- Generated by nerve-contract-codegen.mjs. Do not edit by hand.
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Nerve = require(ReplicatedStorage.Nerve)
local S = Nerve.Schema

local CrewService = Nerve.CreateService({
	Name = "CrewService",
	Client = {
		CrewBankUpdated = Nerve.Signal(S.Tuple({ S.Float64 }), { Direction = "server" }),
		CrewBroadcastSent = Nerve.Signal(S.Tuple({ S.Struct({ id = S.String, author = S.String, message = S.String, timestamp = S.Float64 }) }), { Direction = "server" }),
	},
	Network = {
		GetCrew = Nerve.Method({ Request = S.Struct({  }), Response = S.Tuple({ S.Struct({ crewId = S.String, name = S.String, tag = S.String, reputation = S.Float64, bankBalance = S.Float64, members = S.Array(S.Struct({ id = S.String, name = S.String, rank = S.String, online = S.Boolean, lastActive = S.Float64 })), turfs = S.Array(S.Struct({ id = S.String, name = S.String, controlPct = S.Float64, status = S.String, incomePerHour = S.Float64, x = S.Float64, y = S.Float64 })), broadcasts = S.Array(S.Struct({ id = S.String, author = S.String, message = S.String, timestamp = S.Float64 })) }) }) }),
		DepositBank = Nerve.Method({ Request = S.Struct({ amount = S.Float64 }), Response = S.Tuple({ S.Boolean, S.Optional(S.String), S.Optional(S.Float64) }) }),
		PromoteMember = Nerve.Method({ Request = S.Struct({ memberId = S.String, newRank = S.String }), Response = S.Tuple({ S.Boolean, S.Optional(S.String) }) }),
		BroadcastAlert = Nerve.Method({ Request = S.Struct({ message = S.String }), Response = S.Tuple({ S.Boolean, S.Optional(S.String), S.Optional(S.Struct({ id = S.String, author = S.String, message = S.String, timestamp = S.Float64 })) }) }),
	},
})

function CrewService.Client:GetCrew(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function CrewService.Client:DepositBank(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function CrewService.Client:PromoteMember(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function CrewService.Client:BroadcastAlert(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function CrewService:NerveInit()
end

function CrewService:NerveStart()
end

return CrewService
