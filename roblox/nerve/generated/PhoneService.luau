--!strict

-- Generated by nerve-contract-codegen.mjs. Do not edit by hand.
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Nerve = require(ReplicatedStorage.Nerve)
local S = Nerve.Schema

local PhoneService = Nerve.CreateService({
	Name = "PhoneService",
	Client = {
		MessageReceived = Nerve.Signal(S.Tuple({ S.String, S.String }), { Direction = "server" }),
		CallStateChanged = Nerve.Signal(S.Tuple({ S.String, S.String }), { Direction = "server" }),
		PhoneSettingsChanged = Nerve.Signal(S.Tuple({ S.Struct({ airplaneMode = S.Boolean, wifiEnabled = S.Boolean, bluetoothEnabled = S.Boolean, darkMode = S.Boolean, ringtone = S.String }) }), { Direction = "server" }),
	},
	Network = {
		GetPhoneState = Nerve.Method({ Request = S.Tuple({  }), Response = S.Tuple({ S.Struct({ phoneNumber = S.String, battery = S.Float64, contacts = S.Array(S.Struct({ id = S.String, name = S.String, number = S.String })), conversations = S.Array(S.Struct({ contactId = S.String, lastMessage = S.String, time = S.String, unread = S.Int32 })), settings = S.Struct({ airplaneMode = S.Boolean, wifiEnabled = S.Boolean, bluetoothEnabled = S.Boolean, darkMode = S.Boolean, ringtone = S.String }) }) }) }),
		GetMessages = Nerve.Method({ Request = S.Tuple({ S.String }), Response = S.Tuple({ S.Array(S.Struct({ id = S.String, body = S.String, fromPlayer = S.Boolean, time = S.String })) }) }),
		SendMessage = Nerve.Method({ Request = S.Struct({ number = S.String, body = S.String }), Response = S.Tuple({ S.Boolean, S.Optional(S.String) }), RateLimit = { Requests = 12, Window = 1 } }),
		StartCall = Nerve.Method({ Request = S.Tuple({ S.String }), Response = S.Tuple({ S.Boolean, S.Optional(S.String) }), RateLimit = { Requests = 6, Window = 1 } }),
		EndCall = Nerve.Method({ Request = S.Tuple({  }), Response = S.Tuple({ S.Boolean, S.Optional(S.String) }) }),
		SavePhoneSettings = Nerve.Method({ Request = S.Struct({ airplaneMode = S.Boolean, wifiEnabled = S.Boolean, bluetoothEnabled = S.Boolean, darkMode = S.Boolean, ringtone = S.String }), Response = S.Tuple({ S.Boolean, S.Optional(S.String) }), RateLimit = { Requests = 10, Window = 1 } }),
	},
})

function PhoneService.Client:GetPhoneState(_player: Player)
	-- Implement the authoritative server behavior here.
	return nil
end

function PhoneService.Client:GetMessages(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function PhoneService.Client:SendMessage(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function PhoneService.Client:StartCall(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function PhoneService.Client:EndCall(_player: Player)
	-- Implement the authoritative server behavior here.
	return nil
end

function PhoneService.Client:SavePhoneSettings(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function PhoneService:NerveInit()
end

function PhoneService:NerveStart()
end

return PhoneService
