--!strict

-- Generated by nerve-contract-codegen.mjs. Do not edit by hand.
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Nerve = require(ReplicatedStorage.Nerve)
local S = Nerve.Schema

local MediaService = Nerve.CreateService({
	Name = "MediaService",
	Client = {
		MediaChanged = Nerve.Signal(S.Tuple({ S.String }), { Direction = "server" }),
	},
	Network = {
		GetMediaList = Nerve.Method({ Request = S.Struct({ filter = S.Optional(S.String), favoritesOnly = S.Optional(S.Boolean) }), Response = S.Tuple({ S.Array(S.Struct({ id = S.String, mediaType = S.String, url = S.String, thumbnailUrl = S.Optional(S.String), favorite = S.Boolean, createdAt = S.Float64, title = S.String, location = S.Optional(S.String) })), S.Struct({ all = S.Float64, photos = S.Float64, videos = S.Float64, favorites = S.Float64 }) }) }),
		CaptureMedia = Nerve.Method({ Request = S.Struct({ mediaType = S.String, url = S.String, title = S.Optional(S.String), location = S.Optional(S.String) }), Response = S.Tuple({ S.Boolean, S.Optional(S.Struct({ id = S.String, mediaType = S.String, url = S.String, thumbnailUrl = S.Optional(S.String), favorite = S.Boolean, createdAt = S.Float64, title = S.String, location = S.Optional(S.String) })), S.Optional(S.String) }) }),
		ToggleFavorite = Nerve.Method({ Request = S.Struct({ id = S.String }), Response = S.Tuple({ S.Boolean, S.Boolean, S.Optional(S.String) }) }),
		DeleteMedia = Nerve.Method({ Request = S.Struct({ ids = S.Array(S.String) }), Response = S.Tuple({ S.Boolean, S.Optional(S.String) }) }),
	},
})

function MediaService.Client:GetMediaList(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function MediaService.Client:CaptureMedia(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function MediaService.Client:ToggleFavorite(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function MediaService.Client:DeleteMedia(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function MediaService:NerveInit()
end

function MediaService:NerveStart()
end

return MediaService
