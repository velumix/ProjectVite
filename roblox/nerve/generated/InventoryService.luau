--!strict

-- Generated by nerve-contract-codegen.mjs. Do not edit by hand.
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Nerve = require(ReplicatedStorage.Nerve)
local S = Nerve.Schema

local InventoryService = Nerve.CreateService({
	Name = "InventoryService",
	Client = {
		MoneyChanged = Nerve.Signal(S.Tuple({ S.Float64 })),
		InventoryChanged = Nerve.Signal(S.Tuple({ S.String, S.Int32 })),
		EquipItem = Nerve.Signal(S.Tuple({ S.String }), { Direction = "client" }),
	},
	Network = {
		BuyItem = Nerve.Method({ Request = S.Tuple({ S.String }), Response = S.Tuple({ S.Boolean, S.Optional(S.String), S.Optional(S.Float64) }), RateLimit = { Requests = 12, Window = 1 } }),
		GetInventory = Nerve.Method({ Request = S.Tuple({  }), Response = S.Tuple({ S.Array(S.Struct({ ItemId = S.String, Quantity = S.Int32 })) }) }),
	},
})

function InventoryService.Client:BuyItem(_player: Player, value1)
	-- Implement the authoritative server behavior here.
	return nil
end

function InventoryService.Client:GetInventory(_player: Player)
	-- Implement the authoritative server behavior here.
	return nil
end

function InventoryService:NerveInit()
end

function InventoryService:NerveStart()
end

return InventoryService
